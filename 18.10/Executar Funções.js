
    function multiplica(num1,num2) {
        let resultado = num1 * num2;
        return resultado;
    }

    function divide(num1,num2) {
        let resultado = num1 / num2;
        return resultado;
    }

    function soma(num1,num2) {
        let resultado = num1 + num2;
        return resultado;
    }

    function subtração(num1,num2) {
        let resultado = num1 - num2;
        return resultado;
    }

    function IMC (peso,altura) {
        let resultado = peso / (altura * altura);
        return resultado;
    }

    function desconto(num1,num2) {
        let resultado = num1 - num2;
        return resultado;
    }

    function Salario (num1) {
        let resultado = num1 + (num1*0.08);
        return resultado;
    }

    function idade(num1) {
        let resultado = (2022-num1);
        return resultado;
    }